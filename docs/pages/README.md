# Pages Directory

This directory contains page-level Vue components for the browser extension:

- `popup/` - Extension popup page
- `sidepanel/` - Main sidepanel application
- `options/` - Extension options/settings page